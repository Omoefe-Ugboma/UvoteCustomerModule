import { CollapseClickedDirective } from './collapse-clicked.directive';

describe('CollapseClickedDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseClickedDirective();
    expect(directive).toBeTruthy();
  });
});
